# MCP Server Environment Configuration (Testing)
# Using existing project database for testing

# PostgreSQL MCP Server Configuration (using Docker database)
DATABASE_URL=postgresql://apes_user:testpass123@localhost:5432/apes_production
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USERNAME=apes_user
POSTGRES_PASSWORD=testpass123
POSTGRES_DATABASE=apes_production

# Test Database Configuration
TEST_DATABASE_URL=postgresql://apes_user:testpass123@localhost:5432/apes_production
TEST_DATABASE_NAME=apes_production

# Observability MCP Server Configuration (development)
OTEL_SERVICE_NAME=prompt-improver-mcp-test
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
OTEL_RESOURCE_ATTRIBUTES=service.version=1.0.0,environment=development

# Metrics Endpoints (using project defaults)
METRICS_ENDPOINT=http://localhost:8000/metrics
PROMETHEUS_ENDPOINT=http://localhost:8001

# GitHub MCP Server Configuration (will add if GitHub token available)
GITHUB_OWNER=lukemckenzie
GITHUB_REPO=prompt-improver
GITHUB_API_BASE_URL=https://api.github.com

# Environment
ENVIRONMENT=development
DEBUG_MODE=true